---
title: ROS2 Setting
author: Chaewon Kim
date: 2025-12-22
category: Experiment
layout: post
---



2025-12-22 (Monday)
-------------

My computer has installed ubuntu 20.04

내 컴퓨터는 ubuntu 20.04 버전이 깔려있는 상태이다. 

여기서 nav2관련 라이브러리를 깔려니 foxy 버전이 존재하지 않고 있었음. 

그래서 그냥 현재 세팅 다 한 우분투 밀고 다시 22.04 깔기로 결정 


   

### Ubuntu Setting 

Now we will install Ubuntu 22.04 

What u need is
1. USB (bigger then 4GB)

Download Ubuntu Img file and put it in the USB

Then u can install ubuntu in your computer

우분투 22.04 설치 완료!! 

이제 필요한 기능 빠르게 세팅 ㄱㄱ

To install list
1. terminator (완료)
2. vscode (완료)
    2.1 extension 꼭 설치하기 - md관련, c/cpp, python 
3. Slack (완료)
    설치 후 알람 꼭 켜두기 
4. 한글 설정할 때 vscode에 한글 입력할 수 있도록 신경쓰기 (완료)
5. 깃 블로그 clone해오기 (완료)


### ROS2 Install 

이제 본격적으로 ROS2 설치 시작 하기 
아래 명령어 순차 입력 

```
sudo apt update && sudo apt install locales
sudo locale-gen ko_KR ko_KR.UTF-8
sudo update-locale LC_ALL=ko_KR.UTF-8 LANG=ko_KR.UTF-8
export LANG=ko_KR.UTF-8


# 필수 도구 설치
sudo apt install software-properties-common
sudo add-apt-repository universe

# GPG 키 설정
sudo apt update && sudo apt install curl -y
sudo curl -sSL https://raw.githubusercontent.com/ros/rosdistro/master/ros.key -o /usr/share/keyrings/ros-archive-keyring.gpg

# 저장소 목록에 추가
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/ros-archive-keyring.gpg] http://packages.ros.org/ros2/ubuntu $(. /etc/os-release && echo $UBUNTU_CODENAME) main" | sudo tee /etc/apt/sources.list.d/ros2.list > /dev/null


sudo apt update
sudo apt upgrade
# Desktop 설치 (GUI 툴, 데모 등 포함)
sudo apt install ros-humble-desktop

#환경 변수 설정
echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc
source ~/.bashrc

# 빌드용
sudo apt install python3-colcon-common-extensions

# 모니터링용
sudo apt install htop nvtop

# 그 후 에러 뜨면 에러 로고 보고 필요한거 추가 설치 진행 (의존성 관리 등등 )

# 설치 후 빌드 진행 
colcon build --symlink-install 

```

ROS2 Humble 설치 완!


### 앞으로의 계획 
ros2에 대해 추가적으로 알아본 후, ros2에서 돌아갈 수 있는거 많이 돌려보기 

1. 간단한 통신 talker and listener 돌려보기 
2. Nav2
3. Vision
4. Robot Arm
5. SLAM

### Run Nav2 with TURTLEBOT


### Profile nav2



[1]: https://pages.github.com